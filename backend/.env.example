# Backend local persistence (SQLite)
#
# Recommended for development:
# - DB stored inside the repo (gitignored)
# - Path is resolved relative to the backend/ directory
STORE_SQLITE_PATH=data/app.dev.db

# Optional:
# - Use SQLite (default) or disable persistence (memory)
# STORE_BACKEND=sqlite
# STORE_BACKEND=memory

